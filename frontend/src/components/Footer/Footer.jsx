import styles from "./Footer.module.scss";

const Footer = () => {
  const {
    navContainer,
    listItemContainer,
    listItem,
    linkItem,
    logInContainer,
    logInBtn,
  } = styles;

  return (
    <div>Footer</div>
  );
};

export default Footer;

