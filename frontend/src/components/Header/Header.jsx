import styles from "./Hader.module.scss";

const Header = () => {
  const {
    navContainer,
    listItemContainer,
    listItem,
    linkItem,
    logInContainer,
    logInBtn,
  } = styles;

  return (
    <div>Header</div>
  );
};

export default Header;


